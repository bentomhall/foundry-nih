<section>
    {{~#if hasConcentration}}
    <fieldset>
        <legend>{{ localize "NIH.Concentration" }}</legend>
        {{#each fields}}
            {{ formField field name=name value=value options=options input=input }}
        {{/each}}
    </fieldset>
    {{> "nih.activity-usage-notes" }}
    {{/if~}}
</section>
