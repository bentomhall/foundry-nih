<section class="tab activity-{{ tab.id }} {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">
    <fieldset {{ disabled disabled.activation }}>
        <legend>
            <label class="checkbox">
                {{ localize "NIH.ACTIVATION.FIELDS.activation.label" }}
                {{#if (or activity.activation.canOverride activity.isRider)}}
                {{ formInput fields.activation.fields.override value=source.activation.override
                             input=inputs.createCheckboxInput
                             dataset=(nih-object tooltip="NIH.ACTIVATION.FIELDS.activation.override.hint")
                             ariaLabel=(localize "NIH.ACTIVATION.FIELDS.activation.override.hint") }}
                {{/if}}
            </label>
        </legend>
        {{> "nih.field-activation" activation=activity.activation fields=fields.activation.fields data=data.activation
            activationTypes=activationTypes inputs=inputs label="NIH.ACTIVATION.FIELDS.activation.type.label" }}
    </fieldset>

    <fieldset {{ disabled disabled.duration }}>
        <legend>
            <label class="checkbox">
                {{ localize "NIH.DURATION.FIELDS.duration.label" }}
                {{#if (or activity.duration.canOverride activity.isRider)}}
                {{ formInput fields.duration.fields.override value=source.duration.override
                             input=inputs.createCheckboxInput
                             dataset=(nih-object tooltip="NIH.DURATION.FIELDS.duration.override.hint")
                             ariaLabel=(localize "NIH.DURATION.FIELDS.duration.override.hint") }}
                {{/if}}
            </label>
        </legend>
        {{> "nih.field-duration" duration=activity.duration fields=fields.duration.fields data=data.duration
            durationUnits=durationUnits inputs=inputs }}
        {{ formField fields.duration.fields.concentration value=data.duration.concentration
           input=inputs.createCheckboxInput rootId=partId }}
    </fieldset>
</section>
