<div class="form-group">
    <label>{{ localize "NIH.ACTIVITY.FIELDS.effects.element.level.label" }}</label>
    <div class="form-fields range-fields">
        {{ formInput fields.level.fields.min name=(concat prefix "level.min") value=data.level.min
                     placeholder="0" ariaLabel=(localize "NIH.LevelLimit.Min")
                     input=@root.inputs.createNumberInput }}
        <nih-icon src="systems/nih/icons/svg/range-connector.svg" class="sep"></nih-icon>
        {{ formInput fields.level.fields.max name=(concat prefix "level.max") value=data.level.max
                     placeholder="∞" ariaLabel=(localize "NIH.LevelLimit.Max") }}
    </div>
    <p class="hint">{{ localize "NIH.ACTIVITY.FIELDS.effects.element.level.hint" }}</p>
</div>
