<fieldset>
    <legend>{{ localize "NIH.ACTIVITY.FIELDS.visibility.label" }}</legend>
    {{> "nih.fieldlist" visibilityFields }}
    <div class="form-group">
        <label>{{ localize "NIH.ACTIVITY.FIELDS.visibility.level.label" }}</label>
        <div class="form-fields range-fields">
            {{ formInput fields.visibility.fields.level.fields.min value=source.visibility.level.min
                         placeholder="0" ariaLabel=(localize "NIH.LevelLimit.Min")
                         input=@root.inputs.createNumberInput }}
            <nih-icon src="systems/nih/icons/svg/range-connector.svg" class="sep"></nih-icon>
            {{ formInput fields.visibility.fields.level.fields.max value=source.visibility.level.max
                         placeholder="∞" ariaLabel=(localize "NIH.LevelLimit.Max")
                         input=@root.inputs.createNumberInput }}
        </div>
        <p class="hint">{{ localize "NIH.ACTIVITY.FIELDS.visibility.level.hint" }}</p>
    </div>
    {{#if (or (not activity.isSpell) activity.isRider)}}
    {{ formField fields.visibility.fields.identifier value=source.visibility.identifier rootId=partId }}
    {{/if}}
</fieldset>
