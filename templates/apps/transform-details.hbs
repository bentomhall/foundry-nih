<div>
    {{> ".actor" actor=hostActor class="host" }}
    <nih-icon src="systems/nih/icons/svg/range-connector.svg"></nih-icon>
    {{> ".actor" actor=sourceActor class="source" }}
</div>

{{#*inline ".actor"}}
<div class="{{ class }}">
    <img src="{{ actor.artwork }}" alt="{{ actor.name }}">
    <span class="name">{{ actor.name }}</span>
</div>
{{/inline}}
