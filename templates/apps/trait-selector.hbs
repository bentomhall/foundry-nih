<form autocomplete="off" onsubmit="event.preventDefault();">
    {{> "nih.trait-list" prefix="choices"}}

    {{#if customPath}}
        <div class="form-group stacked">
            <label>{{localize "NIH.TraitSelectorSpecial"}}</label>
            <input type="text" name="{{customPath}}" value="{{custom}}">
        </div>
    {{/if}}

    {{#if bypassesPath}}
        <h3>{{localize "NIH.DamagePhysicalBypass"}}</h3>
        <p class="note">{{localize "NIH.DamagePhysicalBypassHint"}}</p>
        {{> "nih.trait-list" choices=bypasses prefix="bypasses"}}
    {{/if}}

    <button type="submit"><i class="far fa-save"></i> {{localize "NIH.TraitSave"}}</button>
</form>
