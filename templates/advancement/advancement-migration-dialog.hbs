<fieldset class="card items table">
    <legend>{{ localize "NIH.ADVANCEMENT.Migration.Selection" }}</legend>
    <div class="form-group hint">
        <p class="hint">{{ localize "NIH.ADVANCEMENT.Migration.Hint" name=item.name }}</p>
    </div>
    <div class="header">
        <div class="item-name">{{ localize "NIH.AdvancementTitle" }}</div>
        <div class="item-controls">{{ localize "NIH.Add" }}</div>
    </div>
    <ol class="items-list unlist">
        {{#each advancements}}
        <li class="advancement-item item flexrow">
            <div class="item-name flexrow">
                {{#if svg}}
                <nih-icon src="{{ icon }}"></nih-icon>
                {{else}}
                <img class="gold-icon" src="{{ icon }}" alt="{{ title }}">
                {{/if}}
                <span class="name">{{ title }}</span>
            </div>
            <div class="item-controls flexrow">
                <nih-checkbox name="{{ id }}" checked></nih-checkbox>
            </div>
            {{#if summary}}
            <div class="item-summary">
                {{{ summary }}}
            </div>
            {{/if}}
        </li>
        {{/each}}
    </ol>
</fieldset>
