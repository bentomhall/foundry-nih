<fieldset>
    <legend>
        {{#if (eq item.type "weapon")}}{{ localize "NIH.ItemSiegeProperties" }}
        {{else if (eq item.type "equipment")}}{{ localize "NIH.ItemVehicleProperties" }}{{/if}}
    </legend>

    {{!-- Armor Class --}}
    {{ formField fields.armor.fields.value value=source.armor.value localize=true rootId=partId }}

    {{!-- Crew --}}
    <div class="form-group split-group">
        <label>{{ localize "NIH.VEHICLE.MOUNTABLE.FIELDS.crew.label" }}</label>
        <div class="form-fields">

            {{!-- Max --}}
            {{ formField fields.crew.fields.max value=source.crew.max label="NIH.Max" placeholder="—" localize=true
                         classes="label-top" rootId=partId }}

            {{!-- Cover --}}
            {{ formField fields.cover value=source.cover label="NIH.VEHICLE.MOUNTABLE.FIELDS.cover.label"
                         hint="NIH.VEHICLE.MOUNTABLE.FIELDS.cover.hint" localize=true classes="label-top"
                         rootId=partId options=coverOptions }}

        </div>
    </div>

    {{!-- Hit Points --}}
    <div class="form-group split-group">
        <label>{{ localize "NIH.HitPoints" }}</label>
        <div class="form-fields">

            {{!-- Current --}}
            {{ formField fields.hp.fields.value value=source.hp.value label="NIH.Current" placeholder="0"
                         localize=true classes="label-top" rootId=partId }}

            {{!-- Max --}}
            {{ formField fields.hp.fields.max value=source.hp.max label="NIH.Max" placeholder="0" localize=true
                         classes="label-top" rootId=partId }}

            {{!-- Threshold --}}
            {{ formField fields.hp.fields.dt value=source.hp.dt label="NIH.Threshold" placeholder="—" localize=true
                         classes="label-top" rootId=partId }}
        </div>

        {{!-- Conditions --}}
        {{ formInput fields.hp.fields.conditions value=source.hp.conditions input=inputs.createTextInput
                     placeholder=(localize "NIH.VEHICLE.MOUNTABLE.FIELDS.hp.conditions.label") localize=true
                     classes="full-width" rootId=partId }}
    </div>

    {{!-- Speed --}}
    {{#if (eq item.type "equipment")}}
    <div class="form-group split-group">
        <label>{{ localize "NIH.Speed" }}</label>
        <div class="form-fields">

            {{!-- Value --}}
            {{ formField fields.speed.fields.value value=source.speed.value placeholder="0" label="NIH.Value"
                         localize=true classes="label-top" rootId=partId }}

            {{!-- Units --}}
            {{ formField fields.speed.fields.units value=source.speed.units label="NIH.Unit" localize=true
                         classes="label-top" rootId=partId options=unitsOptions }}
        </div>

        {{!-- Conditions --}}
        {{ formInput fields.speed.fields.conditions value=source.speed.conditions input=inputs.createTextInput
                     placeholder=(localize "NIH.VEHICLE.MOUNTABLE.FIELDS.speed.conditions.label") localize=true
                     classes="full-width" rootId=partId }}
    </div>
    {{/if}}
</fieldset>
