<section>
    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.NIH.Subclass.FIELDS.item.label" }}</label>
        <div class="form-fields">
            <ol class="items-list">
                {{#if document.system.item}}
                <li class="item flexrow" data-item-uuid="{{ document.system.item }}" data-item-type="linked">
                    <div class="item-name">{{{ nih-linkForUuid document.system.item }}}</div>
                    <div class="item-controls flexrow">
                        <a class="item-control" data-action="deleteItem" data-tooltip
                           aria-label="{{ localize 'NIH.ItemDelete' }}">
                            <i class="fa-solid fa-trash" inert></i>
                        </a>
                    </div>
                </li>
                {{else}}
                <li class="item flexrow">{{ localize "JOURNALENTRYPAGE.NIH.Subclass.ItemHint" }}</li>
                {{/if}}
            </ol>
        </div>
    </div>

    {{ formField systemFields.style value=system.style options=styleOptions }}

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.NIH.Subclass.FIELDS.description.value.label" }}</label>
        <div class="form-fields">
            <button data-action="launchTextEditor" data-target="system.description.value">
                <i class="fa-solid fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.NIH.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.NIH.Subclass.FIELDS.description.value.hint" }}</p>
    </div>
</section>
