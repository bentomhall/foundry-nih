<section>
    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.NIH.SpellList.Type.Label" }}</label>
        <div class="form-fields">
            <select name="system.type">
                {{ selectOptions CONFIG.spellListTypes selected=system.type }}
            </select>
        </div>
    </div>

    <div class="form-group">
        <label>{{ localize "NIH.Identifier" }}</label>
        <div class="form-fields">
            <input type="text" name="system.identifier" value="{{system.identifier}}">
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.NIH.SpellList.IdentifierHint" }}
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.NIH.SpellList.Grouping.Label" }}</label>
        <div class="form-fields">
            <select name="system.grouping">
                {{ selectOptions GROUPING_MODES selected=system.grouping localize=true }}
            </select>
        </div>
        <p class="hint">
            {{ localize "JOURNALENTRYPAGE.NIH.SpellList.Grouping.Hint" }}
        </p>
    </div>

    <div class="form-group">
        <label>{{ localize "NIH.Description" }}</label>
    </div>
</section>
