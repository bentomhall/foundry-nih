<section class="flexcol">
    <div class="note info">
        {{ localize (ifThen isGroup "NIH.REST.Short.Hint.Group" "NIH.REST.Short.Hint.Normal") }}
    </div>

    {{#if fields.length}}
    <fieldset>
        <legend>{{ localize "NIH.REST.Configuration" }}</legend>
        {{> "nih.fieldlist" fields }}
    </fieldset>
    {{/if}}

    {{#if hitPoints.length}}
    <fieldset>
        <legend>{{ localize "NIH.HitPoints" }}</legend>
        {{> "nih.fieldlist" hitPoints }}
    </fieldset>
    {{/if}}

    {{#if hitDice}}
    {{#if hitDice.canRoll}}
    <fieldset>
        <legend>{{ localize "NIH.HitDice" }}</legend>
        <div class="form-fields">
            <select name="denom">
                {{ selectOptions hitDice.options selected=hitDice.denomination }}
            </select>
            <button type="button" class="dice-button" data-action="rollHitDie" data-tooltip
                    aria-label="{{ localize 'NIH.Roll' }}">
                <i class="fa-solid fa-dice-d20" inert></i>
            </button>
        </div>
        {{ formField autoRoll name="autoHD" value=config.autoHD input=inputs.createCheckboxInput rootId=partId }}
    </fieldset>
    {{else}}
    <div class="note warn">
        {{ localize "NIH.REST.HitDice.None" }}
    </div>
    {{/if}}
    {{/if}}

    {{#if request}}
    {{> "systems/nih/templates/actors/rest/rest-request.hbs" }}
    {{/if}}
</section>
