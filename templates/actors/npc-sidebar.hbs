<div class="sidebar">

    {{!-- HD --}}
    {{#if important}}
    {{#with system.attributes.hd}}
    <div class="pills-group empty">
        {{!-- TODO: Allow clicking this to roll a hit die --}}
        <h3 class="icon">
            <i class="fa-solid fa-dice-d{{ denomination }}"></i>
            <span class="roboto-upper">{{ localize "NIH.HitDice" }}</span>
            <span class="counter">{{ value }} &sol; {{ max }}</span>
        </h3>
    </div>
    {{/with}}
    {{/if}}

    {{!-- Speed --}}
    {{> "nih.actor-trait-pills" values=speed key="speed" icon="fas fa-shoe-prints"
        label="NIH.Speed" configAction="movement" }}

    {{!-- Skills --}}
    <div class="pills-group {{#unless skills}}empty{{/unless}}">
        <h3 class="icon">
            <i class="fas fa-briefcase" inert></i>
            <span class="roboto-upper">{{ localize "NIH.Skills" }}</span>
            {{#if editable}}
            <button type="button" class="config-button unbutton" data-action="showConfiguration" data-config="skills"
                    data-tooltip="NIH.SkillsConfig" aria-label="{{ localize 'NIH.SkillsConfig' }}">
                <i class="fas fa-cog" inert></i>
            </button>
            {{/if}}
        </h3>
        <ul class="pills">
            {{#each skills}}
            <li class="pill">
                <a class="{{ @root.rollableClass }} skill-name" data-key="{{ key }}"
                   data-action="roll" data-type="skill">
                    <span class="label">{{ label }}</span>
                    <span class="separator">&vert;</span>
                    <span class="value">{{ nih-numberFormat total signDisplay="always" }}</span>
                </a>
            </li>
            {{/each}}
        </ul>
    </div>

    {{!-- Senses --}}
    {{> "nih.actor-trait-pills" values=senses key="senses" icon="fas fa-eye"
        label="NIH.Senses" configAction="senses" }}

    {{!-- Resistances --}}
    {{> "nih.actor-trait-pills" values=traits.dr key="dr" color="green" icon="fas fa-shield-halved"
        label="NIH.Resistances" }}

    {{!-- Immunities --}}
    {{#if editable}}
        {{> "nih.actor-trait-pills" values=traits.di key="di" color="green" icon="fas fa-shield"
            label="NIH.DamImm" }}
    {{else}}
        {{> "nih.actor-trait-pills" values=traits.di key="di" color="green" icon="fas fa-shield"
            label="NIH.Immunities" }}
    {{/if}}
    {{> "nih.actor-trait-pills" values=traits.ci key="ci" color="green" svg="rosa-shield"
        label="NIH.ConImm" }}

    {{!-- Vulnerabilities --}}
    {{> "nih.actor-trait-pills" values=traits.dv key="dv" color="maroon" icon="fas fa-heart-crack"
        label="NIH.Vulnerabilities" }}

    {{!-- Damage Modification --}}
    {{> "nih.actor-trait-pills" values=traits.dm key="dm" icon="fas fa-heart-circle-plus"
        label="NIH.DamageModification.Label" }}

    {{!-- Languages --}}
    {{> "nih.actor-trait-pills" values=traits.languages key="languages" icon="fas fa-flag"
        label="NIH.Languages" }}

    {{!-- Habitat --}}
    {{> "nih.actor-trait-pills" values=habitat configAction="habitat" icon="fa-solid fa-mountain-sun"
        label="NIH.Habitat.Configuration.Label" }}

    {{!-- Treasure --}}
    {{!-- TODO: Allow clicking a treasure category pill to roll appropriately for random treasure --}}
    {{> "nih.actor-trait-pills" values=treasure configAction="treasure" icon="fa-solid fa-gem"
        label="NIH.Treasure.Configuration.Label" }}

</div>
