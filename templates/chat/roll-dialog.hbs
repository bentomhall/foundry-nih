<form>
    {{#each formulas}}
    <div class="form-group">
        <label>{{ localize "NIH.Formula" }} {{#if type}}({{type}}){{/if}}</label>
        <input type="text" name="formula" value="{{formula}}" disabled>
    </div>
    {{#if types}}
    <div class="form-group">
        <label>{{ localize "NIH.DamageType" }}</label>
        <select name="roll.{{ @index }}.type">
            {{ selectOptions types }}
        </select>
    </div>
    {{/if}}
    {{/each}}
    {{#if attackModes}}
    <div class="form-group">
        <label>{{ localize "NIH.ATTACK.Mode.Label" }}</label>
        <select name="attackMode">
            {{ selectOptions attackModes }}
        </select>
    </div>
    {{/if}}
    {{#if ammunitionOptions}}
    <div class="form-group">
        <label>{{ localize "NIH.CONSUMABLE.Type.Ammunition.Label" }}</label>
        <select name="ammunition">
            {{ selectOptions ammunitionOptions blank="" }}
        </select>
    </div>
    {{/if}}
    {{#if chooseModifier}}
    <div class="form-group">
        <label>{{ localize "NIH.AbilityModifier" }}</label>
        <select name="ability">
            {{selectOptions abilities selected=defaultAbility labelAttr="label"}}
        </select>
    </div>
    {{/if}}
    {{#if masteryOptions}}
    <div class="form-group">
        <label>{{ localize "NIH.WEAPON.Mastery.Label" }}</label>
        <select name="mastery">
            {{ selectOptions masteryOptions }}
        </select>
    </div>
    {{/if}}
    <div class="form-group">
        <label>{{ localize "NIH.RollSituationalBonus" }}</label>
        <input type="text" name="bonus" value="" placeholder="{{ localize 'NIH.RollExample' }}"/>
    </div>
    <div class="form-group">
        <label>{{ localize "NIH.RollMode" }}</label>
        <select name="rollMode">
            {{selectOptions rollModes selected=defaultRollMode localize=true}}
        </select>
    </div>
</form>
