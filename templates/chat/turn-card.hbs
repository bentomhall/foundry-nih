<div class="chat-card turn-card">
    {{#if actor}}

    {{!-- Deltas --}}
    {{#if deltas.length}}{{> "nih.card-deltas" }}{{/if}}

    {{!-- Actions --}}
    {{#if activities.length}}{{> "nih.card-activities" }}{{/if}}

    {{else}}
    <p>{{ localize "NIH.CHATMESSAGE.TURN.NoCombatant" }}</p>
    {{/if}}
</div>
