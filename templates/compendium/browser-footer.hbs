<div class="flexrow">
    {{~#if displaySelection}}
    <div class="count {{#if invalid}}invalid {{/if}}">
        {{{ localize "NIH.CompendiumBrowser.Selection.Label" summary=summary }}}
    </div>
    <button type="submit"{{#if invalid}} disabled data-tooltip aria-label="{{invalidTooltip}}" {{/if}}>
        {{ localize "NIH.CompendiumBrowser.Selection.Select" }}
    </button>
    {{/if~}}
</div>
